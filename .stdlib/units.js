/*
<javascriptresource>
<menu>hide</menu>
</javascriptresource>
*/

var Units = {
  list: function() {
    return [
      R.string.pixels,
      R.string.points,
      R.string.inches,
      R.string.millimeters,
      R.string.centimeters
    ]
  }
}
