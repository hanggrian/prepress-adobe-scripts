/*
<javascriptresource>
<menu>hide</menu>
</javascriptresource>
*/

/** Assert that document is currently saved. */
function checkSaved() {
    check(document.saved, 'Save the document before proceeding')
}